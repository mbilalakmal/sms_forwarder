name: sms_forwarder
description: An app for forwarding sms to an HTTP endpoint

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  telephony: ^0.1.4
  provider: ^6.0.1
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
